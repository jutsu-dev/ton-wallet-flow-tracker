# Normalize line endings to LF in the repository (host is Windows).
* text=auto eol=lf

# Scripts that must stay LF to run inside Linux containers.
*.sh text eol=lf

# Treat these as binary (no EOL conversion, no diff).
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
